{#
/**
 * @file submit_rdv_entity.html.twig
 * Default theme implementation to present Submit rdv entity data.
 *
 * This template is used when viewing Submit rdv entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_submit_rdv_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('submit_rdv_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
